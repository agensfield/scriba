# Scriba Desktop

Tauri menu bar app placeholder.

The alpha build stops before desktop implementation. This app will consume
snapshots emitted by `@agensfield/scriba`; it should not own usage logic.
